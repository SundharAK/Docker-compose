
services:
  fastapi:
    build: ./app
    ports:
      - "8000:8000"
    depends_on:
      - mysql

  mysql:
    build: ./mysql
    ports:
      - "4306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: testdb

  
